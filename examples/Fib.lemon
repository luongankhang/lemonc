class Fib{
	void main(){
		int k;
		int n;
		n = 12;
		k = fib(n);
		k = fibByIterator(n);
		printf("Calculate Fibonacci sequence recursively, total %d pairs of rabbits after one year\n",k);
		printf("Calculate Fibonacci sequence with loop, total %d pairs of rabbits after one year\n",k);
	}
	// 
	int fib(int n){
		int rs;
		if( n < 3 ){
			rs = 1;
		}else{
			rs = fib(n-1) + fib(n-2);
		}
		return rs;
	}
	
	// 
	int fibByIterator(int month){
		int fm;
		int sm;
		int i;
		int count;
		fm = 1;
		sm = 1;
		i = 2;
		count = 0;
		while( i < month ){
			count = fm + sm;
			fm = sm;
			sm = count;
			i = i + 1;
		}
		return count;
	}
}